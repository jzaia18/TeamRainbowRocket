#include "sharedheader.h"

void server(){
  int clients[2];

  // wait for client 1 to connect
  
  // wait for client 2 to connect

  // fork off to subserver

  // establish connection to both clients

  // relay info between the two clients
}
